# This file will be used to install the packages.
# The command `pip install -e` should be executed in the terminal while being in the directory of this file.

from setuptools import setup, find_packages

setup(
    name="package",
    packages=find_packages()
)